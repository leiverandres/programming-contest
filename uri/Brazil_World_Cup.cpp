#include <bits/stdc++.h>
#define fast ios_base::sync_with_stdio(false); cin.tie(NULL)
using namespace std;

int main() {
  fast;
  int n;
  while (cin >> n) {
    cout << ((n)? "vai ter duas!": "vai ter copa!") << endl;
  }
  return 0;
}
