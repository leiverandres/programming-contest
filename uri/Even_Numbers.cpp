#include <bits/stdc++.h>
#define fast ios_base::sync_with_stdio(false); cin.tie(NULL)
using namespace std;

int main() {
  fast;
  for (int i = 1; i <= 100; i++) {
    if (i%2 == 0) {
      cout << i << endl;
    }
  }
}
