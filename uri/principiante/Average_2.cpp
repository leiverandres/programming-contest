#include <bits/stdc++.h>

using namespace std;

int main() {
  cout << fixed; cout.precision(1);
  double a, b, c, media;
  cin >> a >> b >> c;
  media = ((a*2) + (b*3) + (c*5)) / 10.0;
  cout << "MEDIA = " << media << endl;  
  return 0;
}
